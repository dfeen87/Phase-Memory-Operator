# Phase-Memory Operator – Core Dependencies
# Python >= 3.9

# Core numerical computing (required)
numpy>=1.21,<2.0
scipy>=1.7,<2.0

# Metrics utilities (optional but recommended for validation)
scikit-learn>=1.0,<2.0

# YAML support for locked parameter files (recommended)
pyyaml>=5.4

# Jupyter support (required to run validation notebooks)
jupyter>=1.0
ipykernel>=6.0

# Visualization (used in validation notebooks only)
matplotlib>=3.4
